let numbers = [2,4,6,8,10];
let total = 0;

for(let i = 0; i<numbers.length; i++){
  total += numbers[i];
  }
console.log(`Array Total is ${total}.`);


let arrayAverage = total/numbers.length

console.log(`Array Average is ${arrayAverage}.`);